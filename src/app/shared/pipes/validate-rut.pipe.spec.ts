import { ValidateRutPipe } from './validate-rut.pipe';

describe('ValidateRutPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateRutPipe();
    expect(pipe).toBeTruthy();
  });
});
